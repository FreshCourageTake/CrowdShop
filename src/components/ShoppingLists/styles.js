import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        padding: 30,
        marginTop: 65,
    },
    emptyListMessage: {
        fontSize: 40,
        textAlign: 'center',
        marginBottom: 20
    },
    listContainer: {
        marginBottom: 10
    },
    storeName: {
        marginRight: 10,
        color: 'black'
    },
    subtitle: {
        flexDirection: 'row',
        paddingLeft: 10
    }
});